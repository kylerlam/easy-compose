# ==========================================
# INFRASTRUCTURE CONFIGURATION
# ==========================================
# Timezone (影響定時任務 Cron 節點的執行時間)
TIMEZONE=Asia/Shanghai

# Host Port (外部訪問端口)
PORT_N8N=5678

# Data Persistence WEBHOOK_URL
PATH_N8N_DATA=./n8n_data

# ==========================================
# N8N APPLICATION CONFIGURATION
# ==========================================
# Domain & Webhook Settings
# 注意：WEBHOOK_URL 必須精確，否則 OAuth 回調會失敗
# 域名舉例：DOMAIN_NAME=your-domain.com | WEBHOOK_URL=https://your-domain.com/
# IP 舉例：DOMAIN_NAME=123.123.123.123 | WEBHOOK_URL=https://123.123.123.123/
DOMAIN_NAME=your-domain.com
WEBHOOK_URL=https://your-domain.com/

# Execution Settings
# 開啟後可提升執行性能，但會增加內存消耗
N8N_RUNNERS_ENABLED=true

# ==========================================
# SECURITY & AUTHENTICATION (CRITICAL)
# ==========================================
# [高度重要] 加密密鑰
# 用於加密存儲在 n8n 中的所有憑證 (Credential)。
# 一旦設定，絕對不能丟失或更改，否則所有保存的 API Key 都會失效。
# 建議生成一個強隨機字符串 (例如: openssl rand -hex 24)
N8N_ENCRYPTION_KEY=CHANGE_ME_ENCRYPTION_KEY

# Security Settings
N8N_SECURE_COOKIE=false
N8N_ENFORCE_SETTINGS_FILE_PERMISSIONS=true