services:
  nginx:
    image: nginx:alpine
    container_name: nginx_gateway
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      # 映射配置文件
      - ./conf.d:/etc/nginx/conf.d
      # 映射證書目錄
      - ./certs:/etc/nginx/certs
      # 映射日誌目錄 (可選)
      - ./logs:/var/log/nginx
    networks:
      - nginx_net # 把 Nginx 加入到網絡中

# 👇 2. 這裡定義網絡
networks:
  nginx_net:
    name: nginx_net # 固定名稱，防止加上前綴
    driver: bridge # 橋接模式 網絡管理中心
